pkgname=spi
pkgver=1.1
pkgrel=1
pkgdesc="A simple script to execute commands and log output in Markdown format"
arch=('any')
license=('GPL')
source=('spi.sh')
md5sums=('SKIP')

package() {
    install -Dm755 "$srcdir/spi.sh" "$pkgdir/usr/bin/spi"
}
